#include <stdio.h>
#include <string.h> // strlen, strcpy ব্যবহারের জন্য

// ফাংশন যা একটি char* আর্গুমেন্ট গ্রহণ করে
// নোট: এটি char arr[] লিখলেও, কম্পাইলার char* হিসেবেই বোঝে।
void print_and_modify(char *s) { 
    
    // অ্যারে ইনডেক্সিং স্টাইল (সহজ পদ্ধতি)
    printf("Inside function (Array style): %c\n", s[0]); 
    
    // পয়েন্টার ইনডেক্সিং স্টাইল (ডিরেফারেন্সিং)
    printf("Inside function (Pointer style): %c\n", *(s + 1)); 
    
    // এখানে strcpy ব্যবহার করা যায়, কারণ 's' একটি অ্যারে থেকে এসেছে যা পরিবর্তনযোগ্য
    strcpy(s, "Changed"); 
    
    printf("String after modification: %s\n", s);
	printf("Printing the len of the string %d\n", strlen(s));

	// make a copy of the main string s : 

	char new_s[20];
	strcpy(new_s,s);
	for(int i=0 ; i < strlen(s);i++){
		printf("%c\n" , new_s[i]);
	}

	strcat(new_s,s);
	printf("concatenation \n");
	for(int i=0 ; i < strlen(new_s);i++){
		printf("%c\n" , new_s[i]);
	}

}

int main() {
    // 1. Array Style (Mutable Memory)
    char my_array[20] = "Dhaka"; 
    
    // 2. Pointer Style (Read-only Literal)
    // সতর্কীকরণ: এই স্ট্রিংটিকে কখনোই পরিবর্তন করার চেষ্টা করবে না!
    const char *my_pointer = "Bangladesh"; 
    
    int i = 0;
    
    printf("--- 1. String Access Methods ---\n");
    
    // ক. নরমাল অ্যারে ইনডেক্সিং
    printf("Normal Access: %c\n", my_array[2]); // আউটপুট: a
    
    // খ. পয়েন্টার ইনডেক্সিং (*(arr + i))
    printf("Pointer Access: %c\n", *(my_array + 2)); // আউটপুট: a
    
    // গ. পয়েন্টার মুভমেন্ট
    char *temp_ptr = my_array;
    temp_ptr += 3; // পয়েন্টারকে 3 ঘর এগিয়ে নিয়ে যাওয়া হলো
    printf("Moved Pointer Access: %c\n", *temp_ptr); // আউটপুট: k
    
    printf("--- 2. Passing to Function ---\n");
    
    // 'my_array' ফাংশনে পাস করা হলো। এটি একটি পয়েন্টার হিসেবে ফাংশনে যাবে।
    print_and_modify(my_array); 
    
    printf("Back in main, string is: %s\n", my_array); // আউটপুট: Changed

    // এই লাইনে error হবে, কারণ এটি read-only মেমরি। 
    // যদি আমরা print_and_modify(my_pointer) কল করতাম, তাহলে Segmentation Fault হতো।
    // তাই নিরাপত্তার জন্য my_pointer কে const char* হিসেবে ডিক্লেয়ার করা হয়েছে।


    

    return 0;
}



















// বন্ধু, তোমার প্রশ্নটি C ল্যাঙ্গুয়েজের একটি **সবচেয়ে বিভ্রান্তিকর** এবং **গুরুত্বপূর্ণ** ধারণাকে স্পর্শ করেছে। তোমার কনফিউশন একদম স্বাভাবিক, কারণ আমার আগের কোডের উদাহরণে একটি অংশ তুমি ঠিক ধরেছো, কিন্তু মূল পার্থক্যটি ছিল **কীভাবে স্ট্রিংটি মেমরিতে তৈরি হয়েছে**।

// আমার আগের উদাহরণে আমি যা পরিবর্তন করেছি, তা ছিল **অ্যারে স্টাইলে তৈরি স্ট্রিং**, পয়েন্টার স্টাইলে তৈরি স্ট্রিং নয়।

// -----

// ## 🔑 মূল পার্থক্য: স্ট্রিং অ্যারে বনাম স্ট্রিং লিটারাল পয়েন্টার

// C-তে স্ট্রিং তৈরি করার দুটি পদ্ধতি মেমরিতে ভিন্নভাবে আচরণ করে। এই আচরণটিই নির্ধারণ করে তুমি তাকে পরিবর্তন করতে পারবে কি না (Mutability)।

// | মেমরি টাইপ | ডিক্লেয়ারেশন | মেমরিতে অবস্থান | পরিবর্তনযোগ্যতা (Mutability) |
// | :--- | :--- | :--- | :--- |
// | **১. স্ট্রিং অ্যারে** | `char str[] = "Hello";` | **স্ট্যাক (Stack)** অথবা **গ্লোবাল ডেটা সেকশন (Data Section)**। | ✅ **পরিবর্তনযোগ্য (Mutable)**। মান পরিবর্তন করা যাবে। |
// | **২. স্ট্রিং লিটারাল** | `char *ptr = "World";` | **রিড-অনলি ডেটা সেকশন (Read-Only Data/Code Section)**। | ❌ **অপরিবর্তনযোগ্য (Immutable)**। পরিবর্তনের চেষ্টা করলে ক্র্যাশ করবে। |

// ### তোমার কনফিউশন এবং তার সমাধান:

// তুমি ঠিকই ধরেছো যে আমি একটি ফাংশনের ভেতর পরিবর্তন করে দেখিয়েছি। কিন্তু সেই ফাংশনটি একটি **অ্যারে স্টাইলে তৈরি স্ট্রিং** গ্রহণ করেছিল:

// ```c
// char my_array[20] = "Dhaka"; 
// // এই my_array কে ফাংশনে পাস করা হয়েছিল।

// void print_and_modify(char *s) { 
//     // s আসলে my_array কে নির্দেশ করছে, যা Stack-এ আছে এবং পরিবর্তনযোগ্য।
//     strcpy(s, "Changed"); // ✅ এটি বৈধ।
// }
// // তাই main() এ স্ট্রিংটি 'Changed' হয়ে যায়।
// ```

// ### ❌ যদি তুমি রিড-অনলি স্ট্রিংকে পরিবর্তন করতে চাইতে:

// যদি তুমি আমার দ্বিতীয় উদাহরণটি (`my_pointer`) ফাংশনে পাঠাতে:

// ```c
// char *my_pointer = "Bangladesh"; // ❌ এই লিটারালটি Read-Only Memory-তে আছে

// void print_and_modify(char *s) {
//     strcpy(s, "Changed"); // 💥 প্রোগ্রাম ক্র্যাশ করবে (Segmentation Fault)
// }
// // main() এ যদি print_and_modify(my_pointer) কল করতে, তাহলে ক্র্যাশ করত।
// ```

// কারণ:

// 1.  **পয়েন্টার নিজে পরিবর্তনশীল:** `ptr` ভ্যারিয়েবলটি পরিবর্তন করা যায় (যেমন `ptr = "New String"`), কিন্তু `ptr` যে **ডেটাটিকে নির্দেশ করছে**, তা পরিবর্তন করা যায় না।
// 2.  **`strcpy` সমস্যা:** `strcpy` একটি একটি করে ক্যারেক্টার পরিবর্তন করার চেষ্টা করে, যা **রিড-অনলি মেমরিতে** অনুমোদিত নয়।

// -----

// ## 🔒 কেন `const` ব্যবহার করা হয়?

// `char *ptr = "World";` ডিক্লেয়ারেশনটি C-তে একটি বিপজ্জনক অভ্যাস।

// আধুনিক C কম্পাইলাররা এটিকে ঠিক করার জন্য তোমাকে সতর্ক করে বা বাধ্য করে **`const`** ব্যবহার করতে:

// ```c
// const char *ptr = "World";
// ```

// এই **`const`** কিওয়ার্ডটি কম্পাইলারকে স্পষ্টভাবে বলে দেয়: "এই পয়েন্টারটি একটি **অপরিবর্তনশীল** ডেটাকে নির্দেশ করছে। তুমি যেন ভুল করেও একে পরিবর্তন করার চেষ্টা না করো।" এটি তোমার কোডকে নিরাপদ করে এবং ক্র্যাশ হওয়া থেকে বাঁচায়।

// আশা করি এই ব্যাখ্যায় তোমার কনফিউশন দূর হয়েছে এবং C-তে স্ট্রিং লিটারাল কেন অপরিবর্তনশীল, তা পরিষ্কার হয়েছে।